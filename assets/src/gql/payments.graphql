type CardType {
  id: ID!
  brand: String!
  label: String!
  type: String!
}

enum GatewayAccountProviderName {
  SANDBOX
  BAMBORA
}

enum GatewayAccountType {
  TEST
  LIVE
}

type GatewayAccount {
  id: ID!
  provider_name: GatewayAccountProviderName!
  type: GatewayAccountType!
  service_name: String!
  description: String!
  allow_apple_pay: Boolean!
  allow_google_pay: Boolean!
  allow_zero_amount: Boolean!
  requires_3ds: Boolean!
}
