type User {
  id: ID!
  inserted_at: String!
  updated_at: String!
  platform_admin: Boolean!
  name: String
  email: String
  telephone_number: String
}

type Organisation {
  id: ID!
  name: String!
}

type Service {
  id: ID!
  name: String!
}
