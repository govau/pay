type User {
  external_id: ID!
  inserted_at: String!
  updated_at: String!
  platform_admin: Boolean!
  name: String
  email: String
  telephone_number: String
}

type Organisation {
  external_id: ID!
  name: String!
}

type Service {
  external_id: ID!
  name: String!
}
