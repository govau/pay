const unit = "rem";
const baseAmount = 1;

const small = `${baseAmount * 2}${unit}`;
const medium = `${baseAmount * 3}${unit}`;
const large = `${baseAmount * 4}${unit}`;

export default {
  small,
  medium,
  large
};
