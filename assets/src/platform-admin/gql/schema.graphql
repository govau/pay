# import Organisation, Service from '../../gql/services.graphql'
# import CardType from '../../gql/payments.graphql'

type Query {
  organisations: [Organisation!]!
  services: [Service!]!
  cardTypes: [CardType!]!
}
